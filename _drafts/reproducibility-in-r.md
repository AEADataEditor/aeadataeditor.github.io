- Reference and run renv https://rstudio.github.io/renv/index.html
- Reference and run groundhog https://groundhogr.com/
- Reference remotes::install_version()

Use the same example doi:10.7910/DVN/SVAONZ but remove the `sink()`. Use separate Rmarkdown files for each version, or restart the R session (run separate R calls) for each. 

Reference https://datacolada.org/100 and anything else.

